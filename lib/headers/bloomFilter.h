#include <stdio.h>

void insertAt_bloomFilter(char**, int, int, unsigned char*);
int isItemIn_bloomFilter(char**, int, int, unsigned char*);
void free_bloomFilter(char ***bloomFilter, int N);

int isVaccinated_bloomFilter(char*, int, unsigned char*);
void free_bF(Monitor*);



